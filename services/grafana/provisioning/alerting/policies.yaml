apiVersion: 1

policies:
  - orgId: 1
    receiver: ntfy
    group_by: ["alertname"]
    group_wait: 30s
    group_interval: 5m
    # Household infrastructure: a daily reminder is enough for something already
    # firing, and avoids training the phone to be ignored.
    repeat_interval: 24h
